class Config:
    """Database configuration variables.
    Set to the default values for MySQL databases hosted locally.
    Change if needed.
    """
    DATABASE = {
        'HOST': '127.0.0.1',
        'USER': 'root',
        'PASSWORD': '',
        'SCHEMA': 'university',
        'CHARSET': 'utf8'
        }
